{
  "name": "personify-client",
  "version": "1.0.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node ./build/build.js",
    "start": "npm run build && node ./build/dev-server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
